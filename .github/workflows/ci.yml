name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Build whisper.cpp
        run: ./scripts/build-whisper-lib.sh

      - name: Build
        run: swift build -c release
